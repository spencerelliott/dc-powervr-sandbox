#include "core.h"

unim_result_t unim_init(unim_t *unim, const char* bytes, const char* spritesheet) {
    // Load unim data
    return UNIM_OK;
}

void unim_tick(unim_t *unim) {

}

void unim_play_animation(unim_t* unim, const char* animation) {

}